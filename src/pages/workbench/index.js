import React, { PureComponent } from 'react';

class Workbench extends PureComponent {

  constructor(props) {
    super(props);
    this.state={ };
  }

  render() {
    return (
      <div>工作台</div>
    );
  }
}

export default Workbench;
